import{_ as t,c as e,o as s,ae as n}from"./chunks/framework.Cd-3tpCq.js";const g=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"cinnamon/documenting-tutorial.md","filePath":"cinnamon/documenting-tutorial.md"}'),i={name:"cinnamon/documenting-tutorial.md"};function l(o,a,p,c,r,d){return s(),e("div",null,a[0]||(a[0]=[n(`<h2 id="writing-tutorials" tabindex="-1">Writing tutorials <a class="header-anchor" href="#writing-tutorials" aria-label="Permalink to &quot;Writing tutorials&quot;">​</a></h2><p>It is also possible to write tutorials that are not part of the code. For example, this. They can be found in <code>docs/reference/cinnamon-tutorials/</code>, and are included into the cinnamon documentation via the <code>docs/reference/cinnamon-tutorials/cinnamon-tutorials-docs.sgml.in</code> file. These are plain docbook items, and existing docbook tutorials are suitable.</p><p>This is intended to be a quick guide on the DocBook syntax.</p><h3 id="overall-structure" tabindex="-1">Overall structure <a class="header-anchor" href="#overall-structure" aria-label="Permalink to &quot;Overall structure&quot;">​</a></h3><p>DocBook is a markup language that vaguely resembles HTML, but the tag names are more verbose. A significant difference is that DocBook is more structured, where we have a top level <code>&lt;book&gt;</code>, containing different <code>&lt;parts&gt;</code> that have <code>&lt;chapter&gt;</code>s that in turn contain <code>&lt;section&gt;</code>s. The general structure of a regular DocBook item might look like</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;book&gt;</span></span>
<span class="line"><span>  &lt;part&gt;</span></span>
<span class="line"><span>    &lt;chapter&gt;</span></span>
<span class="line"><span>      &lt;section&gt;</span></span>
<span class="line"><span>        &lt;section&gt;</span></span>
<span class="line"><span>          ...</span></span>
<span class="line"><span>        &lt;/section&gt;</span></span>
<span class="line"><span>        &lt;section&gt;</span></span>
<span class="line"><span>          ...</span></span>
<span class="line"><span>        &lt;/section&gt;</span></span>
<span class="line"><span>      &lt;/section&gt;</span></span>
<span class="line"><span>      &lt;section&gt;</span></span>
<span class="line"><span>        ...</span></span>
<span class="line"><span>      &lt;/section&gt;</span></span>
<span class="line"><span>    &lt;/chapter&gt;</span></span>
<span class="line"><span>    &lt;chapter&gt;</span></span>
<span class="line"><span>      ...</span></span>
<span class="line"><span>    &lt;/chapter&gt;</span></span>
<span class="line"><span>  &lt;/part&gt;</span></span>
<span class="line"><span>  &lt;part&gt;</span></span>
<span class="line"><span>    &lt;chapter&gt;</span></span>
<span class="line"><span>      ...</span></span>
<span class="line"><span>    &lt;/chapter&gt;</span></span>
<span class="line"><span>  &lt;/part&gt;</span></span>
<span class="line"><span>&lt;/book&gt;</span></span></code></pre></div><p>As shown above, sections can be nested. In the example, we have layer 1 sections and layer 2 sections. It is possible to skip section layers and, say, directly include a layer 2 section inside a chapter. This can be done via using the <code>&lt;sect2&gt;</code> instead of the <code>&lt;section&gt;</code> tag (you can specify up to the 5th layer). You can also skip parts or chapters. For example, you might have</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;book&gt;</span></span>
<span class="line"><span>  &lt;part&gt;</span></span>
<span class="line"><span>    &lt;sect2&gt;</span></span>
<span class="line"><span>        ...</span></span>
<span class="line"><span>    &lt;/sect2&gt;</span></span>
<span class="line"><span>    &lt;chapter&gt;</span></span>
<span class="line"><span>      ...</span></span>
<span class="line"><span>    &lt;/chapter&gt;</span></span>
<span class="line"><span>  &lt;/part&gt;</span></span>
<span class="line"><span>&lt;/book&gt;</span></span></code></pre></div><p>This is helpful because sections in layers behave differently. Parts are numbered with Roman numerals. For example, a part might be labeled</p><h3 id="i-code-documentation" tabindex="-1">I. Code documentation <a class="header-anchor" href="#i-code-documentation" aria-label="Permalink to &quot;I. Code documentation&quot;">​</a></h3><p>On the other hand, chapters and sections are not numbered.</p><p>More importantly, using <code>&lt;sect2&gt;</code> directly will affect chunking and the table of contents. Chunking is the act of splitting different sections into different pages when presented to the user. Different parts and chapters are always chunked, and so are layer 1 sections. A section appears on the table of contents if and only if it is a part, a chapter, or a layer 1 section inside a chapter (but a layer 1 section directly inside a part is not displayed).</p><p>So if you wish to use chapters but do not want the layer 1 sections to be chunked, you will want to directly place a layer 2 section inside the chapter. Similarly, if you don&#39;t want your subsections to appear in the table of contents, use a layer 2 section without a layer 1 section.</p><h4 id="escaping-characters" tabindex="-1">Escaping characters <a class="header-anchor" href="#escaping-characters" aria-label="Permalink to &quot;Escaping characters&quot;">​</a></h4><p>Similar to HTML, certain characters have to be escaped. For example, the &gt; symbol is typed as &gt; and &lt; is &lt;. Similarly, the ampersand &amp; has to be escaped as &amp;amp;.</p><h4 id="xi-include" tabindex="-1">xi:include <a class="header-anchor" href="#xi-include" aria-label="Permalink to &quot;xi:include&quot;">​</a></h4><p>It is possible to include content from other xml files via xi:include. The precise syntax is</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;xi:include href=&quot;building.xml&quot;/&gt;</span></span></code></pre></div><p>The path is relative to the top-level document (ie. <code>$(DOC_MODULE)-docs.sgml</code>).</p><p>To use this feature, you need to declare the xi: namespace. This is done via using the following doctype declaration:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;!DOCTYPE book PUBLIC &#39;-//OASIS//DTD DocBook XML V4.3//EN&#39;</span></span>
<span class="line"><span>&#39;http://www.oasis-open.org/docbook/xml/4.3/docbookx.dtd&#39;</span></span>
<span class="line"><span>[</span></span>
<span class="line"><span>&lt;!ENTITY % local.common.attrib &quot;xmlns:xi  CDATA  #FIXED &#39;http://www.w3.org/2003/XInclude&#39;&quot;&gt;</span></span>
<span class="line"><span>]&gt;</span></span></code></pre></div><h3 id="ags-and-attributes" tabindex="-1">ags and attributes <a class="header-anchor" href="#ags-and-attributes" aria-label="Permalink to &quot;ags and attributes&quot;">​</a></h3><h4 id="the-title-tag" tabindex="-1">The &quot;title&quot; tag <a class="header-anchor" href="#the-title-tag" aria-label="Permalink to &quot;The &quot;title&quot; tag&quot;">​</a></h4><p>It is (and recommended) to give each part/chapter/section a title. This can be done via the <code>&lt;title&gt;</code> tag. For example, a section might look like</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;section&gt;</span></span>
<span class="line"><span>  &lt;title&gt;The title&lt;/title&gt;</span></span>
<span class="line"><span>  ...</span></span>
<span class="line"><span>&lt;/section&gt;</span></span></code></pre></div><p>You should give all part/chapter/sections a title. If you cannot think of any sensible title, you probably don&#39;t want a separate section.</p><h4 id="the-para-tag" tabindex="-1">The &quot;para&quot; tag <a class="header-anchor" href="#the-para-tag" aria-label="Permalink to &quot;The &quot;para&quot; tag&quot;">​</a></h4><p>This is the tag you will use the most often. It denotes a paragraph, and is equivalent to the HTML <code>&lt;p&gt;</code> tag. For example,</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;section&gt;</span></span>
<span class="line"><span>  &lt;title&gt;The title&lt;/title&gt;</span></span>
<span class="line"><span>  &lt;para&gt;Lorem ipsum dolor sit amet&lt;/para&gt;</span></span>
<span class="line"><span>&lt;/section&gt;</span></span></code></pre></div><h4 id="the-code-tag" tabindex="-1">The &quot;code&quot; tag <a class="header-anchor" href="#the-code-tag" aria-label="Permalink to &quot;The &quot;code&quot; tag&quot;">​</a></h4><p>To display inline code, you will need the <code>&lt;code&gt;</code> tag. This is functionally the same as the HTML <code>&lt;code&gt;</code> tag. For example,</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;para&gt;Lorem &lt;code&gt;ipsum&lt;/code&gt; dolor sit amet.&lt;/para&gt;</span></span></code></pre></div><p>will be displayed as</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Lorem \`ipsum\` dolor sit amet.</span></span></code></pre></div><h4 id="the-programlisting-tag" tabindex="-1">The &quot;programlisting&quot; tag <a class="header-anchor" href="#the-programlisting-tag" aria-label="Permalink to &quot;The &quot;programlisting&quot; tag&quot;">​</a></h4><p>This is used to display multiple lines of code, such as the examples shown above. For example, the example above is</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;programlisting&gt;</span></span>
<span class="line"><span>    &lt;section&gt;</span></span>
<span class="line"><span>        &lt;title&gt;The title&lt;/title&gt;</span></span>
<span class="line"><span>        &lt;para&gt;Lorem &lt;code&gt;ipsum&lt;/code&gt; dolor sit amet.&lt;/para&gt;</span></span>
<span class="line"><span>    &lt;/section&gt;</span></span>
<span class="line"><span>&lt;/programlisting&gt;</span></span></code></pre></div><p>There are a few things to take note of. Firstly, the &lt; and &gt; items are escaped. Secondly, the contents of the <code>&lt;programlisting&gt;</code> is flushed to the left, regardless of the current indentation of the xml. This is since all whitepsace is rendered verbatim. Thirdly, the end <code>&lt;/programlisting tag&gt;</code> is put on the same row as the last line, or else an extra row will appear in the output.</p><p>If you are intending to show code, you will want to do syntax highlighting. Doing so via the regular DocBook way is useless. Instead, you need some magic, namely enclosing your <code>&lt;programlisting&gt;</code> inside an <code>&lt;informalexample&gt;</code> tag, eg.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;informalexample&gt;</span></span>
<span class="line"><span>  &lt;programlisting&gt;</span></span>
<span class="line"><span>    function foo () {</span></span>
<span class="line"><span>        return bar</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span>  &lt;/programlisting&gt;</span></span>
<span class="line"><span>&lt;informalexample&gt;</span></span></code></pre></div><p>If you do so, then you are allowed to have a line break before the closing <code>&lt;programlisting&gt;</code> tag as well as indent all your code by a common indent to follow the indent of your xml code. gtk-doc will automatically fix all those problems for you (assuming you are using gtk-doc 1.22 or above).</p><p>Note that the language of syntax highlighting is specified on a per-project basis inside the Makefile (for gtk-doc 1.24 or above; it is hardcoded to be always C for gtk-doc 1.23 or below). The syntax highlight for this tutorial is for Javascript, so if you are writing about something that doesn&#39;t resemble Javascript even vaguely (eg. xml), don&#39;t use syntax highlighting.</p><h4 id="the-itemizedlist-tag" tabindex="-1">The &quot;itemizedlist&quot; tag <a class="header-anchor" href="#the-itemizedlist-tag" aria-label="Permalink to &quot;The &quot;itemizedlist&quot; tag&quot;">​</a></h4><p>This generates an itemized list, similar to the HTML <code>&lt;ul&gt;</code> tag. Individual items are put inside <code>&lt;listitem&gt;</code> tags, similar to the HTML <code>&lt;li&gt;</code>. An example usage is as follows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;itemizedlist&gt;</span></span>
<span class="line"><span>  &lt;listitem&gt;</span></span>
<span class="line"><span>    A “&lt;code&gt;metadata.json&lt;/code&gt;” file which contains information about the applet, such as its name, description etc..</span></span>
<span class="line"><span>  &lt;/listitem&gt;</span></span>
<span class="line"><span>  &lt;listitem&gt;</span></span>
<span class="line"><span>    An “&lt;code&gt;applet.js&lt;/code&gt;” file which contains its code.</span></span>
<span class="line"><span>  &lt;/listitem&gt;</span></span>
<span class="line"><span>&lt;/itemizedlist&gt;</span></span></code></pre></div><p>Lists can be nested, and markup can also be used. Sometimes you might want to enclose the contents of each line with a <code>&lt;para&gt;</code></p>`,46)]))}const u=t(i,[["render",l]]);export{g as __pageData,u as default};
