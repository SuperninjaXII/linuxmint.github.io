import{_ as n,c as o,o as i,ae as t}from"./chunks/framework.Cd-3tpCq.js";const h=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"cinnamon/xlet-versioning.md","filePath":"cinnamon/xlet-versioning.md"}'),s={name:"cinnamon/xlet-versioning.md"};function a(r,e,l,d,u,p){return i(),o("div",null,e[0]||(e[0]=[t(`<h2 id="supporting-multiple-versions" tabindex="-1">Supporting multiple versions <a class="header-anchor" href="#supporting-multiple-versions" aria-label="Permalink to &quot;Supporting multiple versions&quot;">​</a></h2><p>With a new version of Cinnamon coming out every 6 months, there might be some cool new feature you want to use in your applet. However, you also don&#39;t want to leave out the poor users using the older versions of Cinnamon.</p><p>Alternatively, when a new version of Cinnamon comes out, your extension is no longer compatible with it. Updating it would, again, leave out the users running older Cinnamons.</p><p>Here extension versioning comes to the rescue. As of Cinnamon 2.6, it is possible to ship different versions of your extension for different versions of Cinnamon. Here &quot;extension&quot; will refer to all extensions, applets and desklets.</p><p>To enable extension versioning, extensions have to add the line</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;multiversion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>in <code>metadata.json</code>. The different versions should be put in different subdirectories. For example, the version for 2.6 should be put in <code>extension@uuid/2.6/</code>. The contents in <code>2.6/</code> should be exactly what you will normally put in <code>extension@uuid/</code>, apart from <code>metadata.json</code>, which should always be put in the parent directory, ie. <code>extension@uuid/</code>.</p><p>You do not need to create a subdirectory for every single Cinnamon version out there. Cinnamon looks for the most recent subdirectory that is not newer than the running Cinnamon version. For example, if you are running 2.6 and there are 2.4, 2.8 directories, the 2.4 directory will be loaded, until the you upgrade to 2.8 in which case the 2.8 directory will be used. Minor version numbers can also be used, eg. 2.6.4, and are sorted accordingly.</p><p>If no suitable directory is found, then the contents in <code>extension@uuid/</code> will be loaded. Note that Cinnamon versions prior to 2.6 will not understand this directory magic, and will always try to load the contents in <code>extension@uuid/</code>. Hence it is suggested that extension maintainers put the version for 2.4 in <code>extension@uuid</code>, and create new directories if changes <em>specific to newer Cinnamon versions</em> are made. Don&#39;t make a new directory whenever a Cinnamon version is out since it is just a waste of space (and maintenance effort).</p><p>Note that this also allows you to support future versions of Cinnamon. For example, if you see a cool feature in the development branch of Cinnamon, you can modify your code and put it in the 2.6 folder (assuming the current version is 2.4). Then once the user upgrades their Cinnamon version, the new code will be used instead. This also allows extensions to be smoothly upgraded between versions.</p>`,10)]))}const m=n(s,[["render",a]]);export{h as __pageData,m as default};
